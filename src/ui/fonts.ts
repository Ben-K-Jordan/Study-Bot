// Font configuration — loaded via Google Fonts CDN at runtime
// CSS variables --font-display and --font-body are set in layout.tsx

export const FONT_DISPLAY = "var(--font-display), 'Caveat', cursive";
export const FONT_BODY = "var(--font-body), 'Patrick Hand', cursive";
